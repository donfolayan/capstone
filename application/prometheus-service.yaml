apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: sock-shop
spec:
  selector:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/component: server
  ports:
    - port: 9090
      targetPort: 9090
      protocol: TCP
      name: http
  type: ClusterIP